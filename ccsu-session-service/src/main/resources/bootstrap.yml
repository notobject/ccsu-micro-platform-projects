server:
  port: 8080
spring:
  application:
    name: ccsu-session-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: @config.instance.id@
  profiles:
    active: @profiles.active@
