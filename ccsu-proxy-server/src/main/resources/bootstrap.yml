server:
  port: 8000
spring:
  application:
    name: ccsu-proxy-server
  cloud:
      config:
        discovery:
          enabled: true
          service-id: ccsu-config-server
  profiles:
    active: @profiles.active@

