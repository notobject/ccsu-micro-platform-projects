server:
  port: 8000
spring:
  application:
    name: ccsu-proxy-server
  cloud:
      config:
        discovery:
          enabled: true
          service-id: @config.instance.id@
  profiles:
    active: @profiles.active@

